import 'package:flutter/material.dart';

class DetailCarousel extends StatelessWidget {
  const DetailCarousel({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
